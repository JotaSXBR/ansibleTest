[defaults]
inventory = inventory.ini

# Disabling host key checking is convenient for development and test environments,
# as it prevents playbook failures when a server is rebuilt with a new SSH key.
# For production environments, it is strongly recommended to set this to 'True'
# to prevent man-in-the-middle (MITM) attacks.
host_key_checking = False 